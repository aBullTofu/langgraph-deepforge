# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

LangGraph learning workspace — experiments with LangGraph for building stateful, multi-actor LLM applications.

## Stack

LangGraph + LangChain, Python. May use Jupyter notebooks for exploration.

## Commands

```bash
# Install dependencies (create venv first if desired)
pip install langgraph langchain

# Run a script
python script.py

# Run a specific test
pytest tests/test_xxx.py::test_name -v
```
